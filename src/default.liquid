<!DOCTYPE html>
<!-- Generated by Anodize. https://github.com/sidmani/anodize -->
<html>
<head>
<meta charset="{{ charset }}">
<title>{{ title }}</title>
<meta name="description" content="{{ description }}">
<meta name="keywords" content="{{ word | join: "," }}">
{% for c in css %}<link rel="stylesheet" href="{{ c }}">
{% endfor %}
{% for r in raw %}{{ r }}
{% endfor %}
</head>
<body>
{{ doc }}
</body>
</html>
